name: Run action

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: install R
      run: sudo apt-get update && sudo apt-get install r-base
